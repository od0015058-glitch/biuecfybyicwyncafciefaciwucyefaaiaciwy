[pytest]
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    error
    # asyncpg + python-dotenv emit ResourceWarning on import inside tests;
    # not interesting and not from our code.
    ignore::ResourceWarning
